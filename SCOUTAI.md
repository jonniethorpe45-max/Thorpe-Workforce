# ScoutAI

This repository currently hosts a **standalone ScoutAI scaffold** at [`scoutai/`](./scoutai/).

## Submit a prompt on the ScoutAI repo

`jonniethorpe45-max/ScoutAI` is not created yet. Follow **[`scoutai/SUBMIT_PROMPT.md`](./scoutai/SUBMIT_PROMPT.md)**:

1. Create the GitHub repo under `jonniethorpe45-max`
2. Grant the Cursor GitHub App access to it
3. Paste the starter prompt from that doc in Cursor mobile / [cursor.com/agents](https://cursor.com/agents)

## Publish the scaffold into that repo

```bash
cd scoutai
bash scripts/create-github-repo.sh
```

That creates `https://github.com/jonniethorpe45-max/ScoutAI` (if needed) and pushes a clean `main` history.

# @scoutai/live

Provider-agnostic live stream adapters for ScoutAI.

Defines `StreamProviderAdapter` for validating sources, playback access, status, and
capabilities. Includes `ExternalLinkAdapter` for externally hosted streams.

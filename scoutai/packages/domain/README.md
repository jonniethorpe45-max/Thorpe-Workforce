# @scoutai/domain

Shared domain enums and value types for ScoutAI Stage 3.

Defines user status, roles, organization types, membership states, recruiter verification,
and guardian relationship status as const objects with matching TypeScript types.

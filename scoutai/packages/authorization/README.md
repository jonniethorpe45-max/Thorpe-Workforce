# @scoutai/authorization

Role-based authorization helpers built on `@scoutai/domain` role types.

Provides policy functions such as `hasRole`, `isScoutAiAdmin`, and `canViewAdminSystemInfo`
that return structured `AuthorizationResult` values for API guards and UI gates.

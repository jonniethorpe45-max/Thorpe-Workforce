# @scoutai/ai

Provider-agnostic AI abstraction for structured generation in ScoutAI.

Defines `AIProvider` and request/response types plus a `MockAIProvider` for local development
and tests. No live vendor SDKs are bundled in this package.

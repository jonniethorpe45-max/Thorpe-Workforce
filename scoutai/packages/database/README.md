# @scoutai/database

Prisma-backed persistence layer for ScoutAI (schema pending).

This package will host the Prisma schema, migrations, seed script, and generated client.
The parent agent will add `prisma/schema.prisma`; until then only package scaffolding exists.

Scripts: `generate`, `migrate:dev`, `migrate:deploy`, `seed`, `build`, and `typecheck`.

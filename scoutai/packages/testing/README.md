# @scoutai/testing

Shared test utilities for ScoutAI packages and applications.

Provides `createFakeUser` for domain-shaped fixtures, `wait` for async timing, and
`expectError` for asserting thrown errors in Vitest suites.

# @scoutai/video

Video storage provider abstraction for upload lifecycle management.

Defines `VideoStorageProvider` for creating, finalizing, and deleting assets, with a
`MockVideoStorageProvider` suitable for integration tests and local development.

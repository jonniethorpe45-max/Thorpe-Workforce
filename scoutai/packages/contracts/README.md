# @scoutai/contracts

HTTP and API contract types shared across ScoutAI services.

Defines the standard API error envelope, auth/session DTOs, health/readiness responses,
and registration/login payloads. Password hashes are never exposed on public types.

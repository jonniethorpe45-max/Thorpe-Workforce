# @scoutai/ui

Minimal React 18 UI primitives for ScoutAI web applications.

Exports `Button`, `Input`, `Card`, and `Badge` components with `className` support and no
required CSS framework. Consumers supply styling via class names or optional inline styles.
